# Routines Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add routines that store reusable agent instructions, run from cron triggers, and can be started manually for testing.

**Architecture:** Persist routines, trigger definitions, and run history in Postgres. Use BullMQ Job Schedulers for cron production and reuse the existing session manager so a routine can create its first session and then keep prompting that same session.

**Tech Stack:** TypeScript, Drizzle, Mercurius GraphQL, BullMQ, Redis, React Relay, Tailwind/shadcn components.

---

### Task 1: Persistence

**Files:**
- Create: `apps/api/src/db/schema/routines.ts`
- Modify: `apps/api/src/db/schema/index.ts`
- Generate: `apps/api/drizzle/*`

Add `routines`, `routine_triggers`, `routine_cron_triggers`, and `routine_runs` with company scope, RLS policies, and indexes for active trigger lookup and run history.

### Task 2: Backend Services

**Files:**
- Create: `apps/api/src/services/routines/*`
- Create: `apps/api/src/workers/routine_triggers.ts`
- Modify: `apps/api/src/server/api_server.ts`

Implement routine CRUD, trigger scheduler synchronization, manual execution, BullMQ queue management, and worker processing. The execution service should create a first session when `session_id` is null, reuse that session later, and skip overlapping runs by default.

### Task 3: GraphQL

**Files:**
- Create: routine query/mutation files under `apps/api/src/graphql/resolvers` and `apps/api/src/graphql/mutations`
- Create: `apps/api/src/graphql/registries/routine_graphql_registry.ts`
- Modify: `apps/api/src/graphql/schema/schema.graphql`
- Modify: `apps/api/src/graphql/graphql_application.ts`
- Modify: `apps/api/src/graphql/registries/graphql_resolver_registry.ts`

Expose list/detail/run history queries plus create/update/delete routine, create/update/delete cron trigger, and manual trigger mutations.

### Task 4: Web UI

**Files:**
- Create: `apps/web/src/pages/routines/*`
- Modify: `apps/web/src/routes.ts`
- Modify: `apps/web/src/components/layout/application_sidebar.tsx`

Add a Routines page with a list, editor dialog, cron trigger controls, run history, and manual Run action that links to the created or reused chat session.

### Task 5: Verification

Run focused API tests for routine services and workers, Relay generation, and web checks. Full API check currently fails on `origin/main`, so document that baseline separately from routines-specific verification.
