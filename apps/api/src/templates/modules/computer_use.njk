## Computer Use Tools

This session is using the {{ environmentProvider }} template {{ environmentTemplateName }} ({{ environmentTemplateId }}), which supports desktop interaction.
Use computer-use tools only when a GUI flow is necessary or materially simpler than the available CLI or API paths.
Capture the screen before acting, keep interactions incremental, and verify visible state after each major UI change.
