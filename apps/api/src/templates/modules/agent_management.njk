## Agent Management Tools

Use agent management tools for explicit create or update requests, or when durable agent configuration has to change.
Keep changes minimal and intentional, and be precise about updates to models, reasoning, secrets, and environment templates.
When a configuration change affects future behavior, summarize the exact before-and-after state.
