## Terminal Tools

Use terminal tools for repository and environment work that is best handled through files, commands, or patches.
Inspect the current state before editing, prefer targeted commands over broad scans, and keep changes small enough to verify.
Read command output before choosing the next action instead of assuming the command succeeded.
