type ModelProviderCredential {
  id: ID!
  companyId: ID!
  name: String!
  modelProvider: String!
  type: String!
  refreshToken: String
  refreshedAt: String
  createdAt: String!
  updatedAt: String!
}
