# Copy this file to `apps/web/.env.local` for local Vite development.

# Clerk publishable key for the local sign-in UI.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me

# Local API GraphQL endpoint.
# If you proxy the API over HTTPS locally, change this to that HTTPS URL.
VITE_GRAPHQL_URL=http://localhost:4000/graphql
